[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'if [ \"$TRACEDB_SERVICE_MODE\" = \"worker\" ]; then exec tracedb-worker; else exec tracedb-server; fi'"
healthcheckPath = "/v1/health"
restartPolicyType = "ON_FAILURE"
