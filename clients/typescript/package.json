{
  "name": "@tracedb/sdk",
  "version": "0.1.0-development",
  "description": "TraceDB public TypeScript SDK over the generated HTTP transport.",
  "private": false,
  "type": "module",
  "license": "MIT",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./transport": {
      "types": "./dist/client.d.ts",
      "default": "./dist/client.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "node scripts/clean-dist.mjs && tsc -p tsconfig.build.json && tsc -p tsconfig.smoke.json && node scripts/rewrite-declaration-imports.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "smoke": "node dist/smoke.js",
    "public-smoke": "node dist/public-sdk-smoke.js",
    "package-smoke": "node dist/package-entry-smoke.js && node dist/build-package-smoke.js",
    "pack-dry-run": "npm pack --dry-run --json",
    "consumer-smoke": "node scripts/packed-consumer-smoke.mjs",
    "http-smoke": "npm run build && node dist/http-smoke.js",
    "public-http-smoke": "npm run build && node dist/public-http-smoke.js",
    "quickstart": "npm run build && node dist/quickstart.js",
    "gateway-smoke": "npm run build && node dist/gateway-smoke.js",
    "check": "npm run typecheck && npm run build && npm run smoke && npm run public-smoke && npm run package-smoke && npm run pack-dry-run && npm run consumer-smoke"
  },
  "devDependencies": {
    "@types/node": "25.9.0",
    "typescript": "6.0.3"
  }
}
